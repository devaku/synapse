# =============================================================================
# SYNAPSE APPLICATION - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your settings
# 
# ENVIRONMENT BEHAVIOR:
# - NODE_ENV=development: Uses values from this .env file (with fallback to defaults)
# - NODE_ENV=production: Values are provided by Kubernetes (ConfigMap/Secrets)
#
# PRODUCTION DEPLOYMENT:
# Variables marked [K8S-CONFIGMAP] → Kubernetes ConfigMap
# Variables marked [K8S-SECRET] → Kubernetes Secret
# Variables marked [HARDCODED] → Built into application (no K8s config needed)
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION [K8S-CONFIGMAP]
# -----------------------------------------------------------------------------
# The environment mode (development/production)
NODE_ENV=development

# Port where the server will run [K8S-CONFIGMAP]
PORT=8080

# Base URL for the server (used for generating links and callbacks), production is hardcoded
# add a value in the .env file for development only
SERVER_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION [K8S-CONFIGMAP]
# -----------------------------------------------------------------------------
# PostgreSQL connection string [K8S-CONFIGMAP]
# Format: postgresql://username:password@host:port/database?schema=schemaname
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/synapse_db?schema=synapse

# -----------------------------------------------------------------------------
# SESSION CONFIGURATION [K8S-SECRET]
# -----------------------------------------------------------------------------
# Secret key for session encryption [K8S-SECRET] 
# IMPORTANT: Change this to a strong random string in production! 
SESSION_SECRET=your-super-secret-session-key-change-me

# -----------------------------------------------------------------------------
# KEYCLOAK AUTHENTICATION (Optional)
# -----------------------------------------------------------------------------
# Keycloak server URL [K8S-CONFIGMAP]
KEYCLOAK_URL=

# Keycloak realm name [K8S-CONFIGMAP]
KEYCLOAK_CLIENT_REALM=

# Keycloak client ID [K8S-CONFIGMAP]
KEYCLOAK_CLIENT_ID=

# Keycloak client secret [K8S-SECRET]
KEYCLOAK_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# RSA ENCRYPTION KEYS [K8S-SECRET]
# -----------------------------------------------------------------------------
# RSA 256 public key for verification [K8S-SECRET]
RSA256_PUBLIC_KEY=

# RSA OAEP public key for encryption [K8S-SECRET]
RSA_OAEP_PUBLIC_KEY=

# -----------------------------------------------------------------------------
# GITHUB INTEGRATION
# -----------------------------------------------------------------------------
# GitHub OAuth App Client ID [K8S-CONFIGMAP]
GITHUB_CLIENT_ID=

# GitHub OAuth App Client Secret [K8S-SECRET]
GITHUB_CLIENT_SECRET=

# GitHub App ID [K8S-CONFIGMAP]
GITHUB_APP_ID=

# Path to GitHub App private key file [K8S-SECRET]
GITHUB_PRIVATE_KEY_PATH=

# GitHub App Installation ID [K8S-CONFIGMAP]
GITHUB_INSTALLATION_ID=

# -----------------------------------------------------------------------------
# AI SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# AI Provider: 'ollama' for local, 'server' for remote [HARDCODED]
AI_PROVIDER=ollama

# AI API URL [HARDCODED]
# - For Ollama: http://localhost:11434
# - For remote server: your-server-url
AI_API_URL=http://localhost:11434

# AI API Key [K8S-SECRET] (only needed for remote server)
AI_API_KEY=

# AI Model name to use [HARDCODED]
AI_MODEL_NAME=phi3

# Enable MCP tools (true/false) [HARDCODED]
# Set to 'false' to disable MCP tools
AI_ENABLE_MCP_TOOLS=true

# Use simple mode for minimal requests (true/false) [HARDCODED]
# Set to 'true' to enable simple mode
AI_USE_SIMPLE_MODE=false

# =============================================================================
# NOTES:
# =============================================================================
# 1. [K8S-SECRET]: Stored in Kubernetes Secret in production (sensitive data)
# 2. [K8S-CONFIGMAP]: Stored in Kubernetes ConfigMap in production (non-sensitive config)
# 3. [HARDCODED]: Built into application, no Kubernetes configuration needed
# 4. In development: All values read from this .env file
# 5. Never commit your .env file to version control
# 6. Keep your secrets secure and rotate them regularly
# =============================================================================
