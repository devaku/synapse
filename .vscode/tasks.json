{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm run build",
            "type": "npm",
            "script": "build",
            "problemMatcher": [
                "$tsc"
            ],
            "options": {
                "cwd": "${workspaceFolder}/backend"
            },
            "isBackground": false,
        }
    ]
}