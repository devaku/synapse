# =============================================================================
# SYNAPSE FRONTEND - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your settings
# 
# ENVIRONMENT BEHAVIOR:
# - NODE_ENV=development: Uses values from this .env file (with fallback to defaults)
# - NODE_ENV=production: Uses hardcoded defaults (ignores .env)
#
# PRODUCTION DEPLOYMENT:
# All variables are [HARDCODED] → Built into application (no K8s config needed)
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION [HARDCODED]
# -----------------------------------------------------------------------------
# Backend server URL [HARDCODED]
VITE_SERVER_URL="http://localhost:8080"

# Backend API URL [HARDCODED]
VITE_API_URL="http://localhost:8080/api/v1"

# WebSocket server URL [HARDCODED]
VITE_SOCKET_URL="http://localhost:8080"

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION [HARDCODED]
# -----------------------------------------------------------------------------
# Frontend application URL [HARDCODED]
VITE_FRONTEND_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# KEYCLOAK AUTHENTICATION [HARDCODED]
# -----------------------------------------------------------------------------
# Keycloak server URL [HARDCODED]
VITE_KEYCLOAK_URL="http://localhost:4000"

# Keycloak realm name [HARDCODED]
VITE_KEYCLOAK_REALM="synapse_realm"

# Keycloak client ID [HARDCODED]
VITE_KEYCLOAK_CLIENT_ID="client_synapse"

# =============================================================================
# NOTES:
# =============================================================================
# 1. [HARDCODED]: All values are built into application in production
# 2. In development: All values read from this .env file
# 3. In production: All values use hardcoded defaults (no Kubernetes needed)
# 4. Never commit your .env file to version control
# 5. Vite requires all env variables to be prefixed with VITE_
# =============================================================================