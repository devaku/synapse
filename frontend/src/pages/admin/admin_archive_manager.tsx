import HeaderContainer from '../../components/container/header_container';

export default function AdminArchiveManagerPage() {
	return (
		<HeaderContainer pageTitle="Admin - Archive Manager">
			<div>Admin Archive Manager Page</div>
		</HeaderContainer>
	);
}
